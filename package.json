{
  "name": "closethegate-chat",
  "version": "1.0.0",
  "type": "module",
  "main": "apps/chat/dist/index.js",
  "scripts": {
    "dev": "cd apps/chat && npm run dev",
    "build": "cd apps/chat && npm install && npm run build",
    "start": "cd apps/chat && npm start"
  },
  "dependencies": {
    "socket.io": "^4.7.4",
    "pg": "^8.11.3",
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.10.9",
    "tsx": "^4.6.2"
  },
  "engines": {
    "node": ">=18"
  }
}
